/**
 * Mehrere Kommentare werden so eingeführt
 */


// Javascript ist Case-Sensitiv
// Nach jeder Anweisung muss ein Semikolon stehen

// Javascript besitzt vier Datentypen Number, String, Boolean und Objekt

// Javascript ist eine lose typisierte Sprache
// Wenn kein Wert zugewiesen wird, dann besitzt die Variable den Wert undefined
var name;

// Ein Block wird mit geschweiften Klammern erstellt
{
	name = "";
	
	if(isNaN(name)) {
		// Das ist eine Funktion
		alert("Name ist keine Zahl");
	}
	
}

function ausgabeHalloWelt() {
	alert("Hallo Welt");
}

